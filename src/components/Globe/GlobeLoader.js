'use client';

/**
 * GlobeLoader — Placeholder exibido enquanto o GlobeView (WebGL) carrega.
 * Retorna null intencionalmente: o fundo escuro já vem do body (#050A18 em globals.css),
 * então não há flash visível para o usuário.
 */
export default function GlobeLoader() {
  return null;
}
